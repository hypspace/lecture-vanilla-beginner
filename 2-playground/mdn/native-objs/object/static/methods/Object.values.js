/* Object.values(obj) */

//  - 설명:
//   - 객체의 모든 열거 가능한 속성 값(value)을 배열로 반환. ECMAScript 2017 (ES8)에서 도입됨

//  - 활용:
//   - 객체의 데이터를 쉽게 다룰 때 유용함

// 1. 사용 예
const obj = { a: 1, b: 2, c: 3 }
console.log(Object.values(obj)) // [1, 2, 3]

// ... Object.keys.js 참고
